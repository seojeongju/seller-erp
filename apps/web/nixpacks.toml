[phases.setup]
nixPkgs = ["nodejs-18_x", "pnpm"]

[phases.install]
cmds = [
  "pnpm install --frozen-lockfile",
  "pnpm --filter @seller-erp/db db:generate"
]

[phases.build]
cmds = ["pnpm --filter @seller-erp/web build"]

[start]
cmd = "pnpm --filter @seller-erp/web start"

[variables]
NODE_ENV = "production"

