{
  "buildCommand": "cd ../.. && npm install && npx prisma generate --schema=./packages/db/prisma/schema.prisma && cd apps/web && npm run build",
  "installCommand": "npm install --legacy-peer-deps",
  "framework": "nextjs",
  "regions": ["icn1"]
}

