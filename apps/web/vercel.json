{
  "buildCommand": "cd ../.. && pnpm --filter @seller-erp/db db:generate && cd apps/web && pnpm build",
  "installCommand": "npm install -g pnpm@8.6.0 && pnpm install --no-frozen-lockfile",
  "framework": "nextjs",
  "regions": ["icn1"]
}

